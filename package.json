{
  "name": "phone-backend",
  "version": "1.0.0",
  "engines": {
    "node": "10.16.2"
  },
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node --inspect index.js",
    "dev": "nodemon --inspect index.js",
    "build:ui": "rm -rf build && cd ../fullstack2020-phone-frontend && npm run build && cp -r build ../phone-backend",
    "deploy": "git push heroku master",
    "deploy:backend": "git add . && git commit -m uibuild && git push && git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "logs:prod": "heroku logs --tail"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.10",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
